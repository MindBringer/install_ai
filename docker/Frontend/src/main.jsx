import React from "react"
import ReactDOM from "react-dom/client"
import RAGFrontend from "./App.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RAGFrontend />
  </React.StrictMode>
)
